import { HTTPModule } from "../interfaces";

/**
 * This class serves as a wrapper to the underlying
 * HTTP request library
 * 
 * example usage: 
 * HTTPRequest.get('endpoint-url')
 * HTTPRequest.post('endpoint-url',data)
 *  
 */
export class HTTPRequest implements HTTPModule {
    
}